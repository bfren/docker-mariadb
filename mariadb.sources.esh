# MariaDB <%= ${MARIADB_MINOR} %> repository list
# https://mariadb.org/download/
X-Repolib-Name: MariaDB
Types: deb
URIs: https://mirror.mariadb.org/repo/<%= ${MARIADB_VERSION} %>/debian https://mirror.nl.mirhosting.net/mariadb/mariadb-<%= ${MARIADB_VERSION} %>/repo/debian
Suites: <%= ${DEBIAN_NAME} %>
Components: main
Signed-By: <%= ${MARIADB_KEYRING} %>
