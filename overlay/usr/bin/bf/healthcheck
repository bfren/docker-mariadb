#!/usr/bin/nu

use bf
use bf-mariadb db
bf env load -x

# Attempt to execute a simple SQL query
export def main [] {
    let check = { echo "SELECT 1;" | ^mariadb ...(db root) | ^head -n1 } | bf handle -i
    if $check == "1" { exit 0 }
    exit 1
}
